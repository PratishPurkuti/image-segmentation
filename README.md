# image-segmentation
a Flask-based web application that performs instance segmentation on an uploaded image and extracts every distinct object instance as a separate PNG file with a transparent background.
